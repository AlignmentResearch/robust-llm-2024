# @package _global_
defaults:
- /dataset: PasswordMatch/main
- /evaluation: DEFAULT
- /attack@evaluation.evaluation_attack: RANDOM_TOKEN  # len 10 is the default now.
- /model: pythia-14m
- _self_

dataset:
  n_val: 10

experiment_name: ???
run_name: ???
experiment_type: "evaluation"
