# @package _global_
defaults:
- /training: DEFAULT
- _self_

training:
  optimizer: "adafactor"
  save_to: DISK
  num_train_epochs: 3
  learning_rate: 1e-5
  lr_scheduler_type: "linear"

dataset:
  n_train: 20_000
  n_val: 200

experiment_type: "training"
