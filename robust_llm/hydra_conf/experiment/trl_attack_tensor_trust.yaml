experiment_type: "training"

dataset:
  dataset_type: "AlignmentResearch/PasswordMatch"
  n_train: 10
  n_val: 10

training:
  num_train_epochs: 1

  iterative:
    iterative_training: true
    training_attack:
      attack_type: "trl"
      train_frequency: null # only train the first round
      trl_attack_config:
        batch_size: 3
        mini_batch_size: 1
        gradient_accumulation_steps: 1
        ppo_epochs: 5
    num_iterative_training_rounds: 5
    skip_first_training_round: false
    num_examples_to_generate_each_round: 50
