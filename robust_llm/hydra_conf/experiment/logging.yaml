experiment_type: "training"
experiment_name: "logging_training_example"

environment:
  model_name_or_path: "bert-base-uncased"
  dataset_type: "tensor_trust"
  train_set_size: 26
  validation_set_size: 26
  shuffle_train_set: true
  shuffle_validation_set: true

training:
  num_train_epochs: 3
  logging_steps: 1
  eval_steps: 10
